import { defineMessages } from 'react-intl';

const scope = 'hitit.containers.pages.home';

export default defineMessages({
    hello: {
        id: `${scope}.msg`,
        defaultMessage: 'Hello world'
    },
    title: {
        id: `${scope}.title`,
        defaultMessage: 'Hit! T'
    }
})