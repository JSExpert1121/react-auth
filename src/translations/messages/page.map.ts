import { defineMessages } from 'react-intl';

const scope = 'hitit.containers.pages.map';
export default defineMessages({
    title: {
        id: `${scope}.title`,
        defaultMessage: 'Map View'
    }
});