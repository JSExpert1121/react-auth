import { defineMessages } from 'react-intl';

const scope = 'hitit.containers.pages.dashboard';
export default defineMessages({
    title: {
        id: `${scope}.title`,
        defaultMessage: 'Dashboard'
    }
});